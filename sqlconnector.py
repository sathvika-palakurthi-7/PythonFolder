from mysql.connector import connect
connection=connect(user="sathvika",
                   password=7396656009,
                   host="localhost",
                   database="StudentFacultyStorage")
